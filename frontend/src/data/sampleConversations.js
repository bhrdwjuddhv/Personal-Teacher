// One placeholder screenshot slot per teacher. Swap `src` for a real
// screenshot path once you have one (drop the file in public/docs/samples/
// with a matching name) — the placeholder disappears once the image loads.
// Add your own `caption` per teacher if you want one; left blank on purpose.
export const sampleConversations = {
  hitesh: { src: '/samples/hitesh-sample.png', caption: '' },
  piyush: { src: '/samples/piyush-sample.png', caption: '' },
}
